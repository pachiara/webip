<div class="container-fluid">
<ol class="breadcrumb col-md-2">
  <li><%= link_to Vlan, vlans_path %></li>
  <li class="active">Vlan IP</li>
</ol>
<div class="col-md-3">
  <%= render 'search_vlan_ip' %>
</div>
<div class="col-md-2">
<%= render 'per_page_vlan_ips' %>
</div>
<%= unless @vlan_ips.nil?; will_paginate @vlan_ips, :renderer => BootstrapPagination::Rails, :class => "pull-right", :inner_window => 2; end %>
</div>
